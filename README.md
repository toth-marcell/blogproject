# Blog project

## web and backend

1. Update `.env` as neccesary
2. `npm i`
3. `node server.js`

If the database is empty, an admin user named "admin" with the password "admin" will be created.

## desktop

Adjust `APIURL` in `API.cs` as neccesary to point to the backend.
